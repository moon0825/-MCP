# 회의록 자동화 MCP 서버 — 리서치 & 설계 문서

> 회의 녹음 파일을 올리면 → 프로젝트 컨텍스트(용어집·참석자)로 정확도를 높인 전사 → 화자 자동 구분 + 회의를 거듭할수록 화자를 학습 → 구조화된 회의록 자동 생성.
> 제약: GPU 없는 저사양 노트북, 한국어 회의가 주 대상, 하이브리드(로컬+클라우드) 허용.

작성일: 2026-07-08 · 상태: 설계 제안 (구현 전)

---

## 1. 리서치 요약 — 기존 프로젝트로 해결되는가?

6개 방향(기존 앱, ASR 엔진, 화자분리/화자학습, MCP 패턴, 컨텍스트 정확도 부스팅, 저사양 전략)을 조사한 결론:

**완성품은 없다. 하지만 조립 부품은 전부 있다.**

### 1.1 기존 오픈소스 프로젝트 비교

| 프로젝트 | 화자분리 | 화자 학습(교차 회의) | 커스텀 용어 | 형태 | 비고 |
|---|---|---|---|---|---|
| **Speakr** | ✅ | ✅ 유일 (voice profiles) | ✅ 폴더별 | Docker 웹앱+REST | AGPL, 알파, WhisperX 백엔드는 GPU 지향 |
| **Vibe** | ✅ (sherpa-onnx, CPU) | ❌ 익명 라벨 | ⚠️ initial_prompt만 | 데스크톱 앱+HTTP API | MIT, 활발히 유지보수 |
| **aTrain** | ✅ (pyannote) | ❌ | ❌ | Windows 앱+CLI | 노트북 CPU 벤치마크 공개 (22분 파일 → 13~26분) |
| **noScribe** | ✅ | ❌ | ❌ | GUI+CLI | CPU에서 오디오의 최대 3배 시간 |
| **Buzz** | ⚠️ dev 버전 | ❌ | ⚠️ --prompt | GUI+CLI | MIT |
| **Meetily** | 💰 유료 티어 | ❌ | ❌ | 데스크톱 | 화자분리가 PRO 페이월 |
| **Speaches** | ❌ | ❌ | ⚠️ prompt | Docker, OpenAI 호환 API | MIT, CPU int8 ASR 서버로 유용 |
| **silverstein/minutes** | ✅ (Rust 네이티브) | ✅ (voices.db) | ❌ | **MCP 서버** | MIT, whisper.cpp 기반 — 가장 유사한 선행 사례 |
| 기타 Whisper MCP 래퍼 다수 | ❌ | ❌ | ❌ | MCP | 단순 전사만 |

### 1.2 핵심 발견 (설계를 결정하는 사실들)

1. **"다음 회의에서 김대리를 자동 인식"은 어떤 클라우드 API도 제공하지 않는다.** 모든 화자분리(pyannote, Clova, AssemblyAI, Deepgram, Gemini)는 파일마다 리셋되는 익명 라벨(Speaker 1/2)만 준다. 교차 회의 화자 식별은 직접 만들어야 하는 유일한 부분 — 그런데 **가장 싼 부분**이다: 화자별 임베딩 추출(CPU 수 초) + SQLite에 저장 + 코사인 유사도 매칭 ≈ 수백 줄 코드.
2. **비싼 부분(ASR·요약)은 이 사용량에선 클라우드가 거의 공짜다.** 주 2~5시간 회의면 Gemini Flash 무료 티어, Groq Whisper 무료 티어 안에 들어간다. 유료로 가도 한국어 특화 엔진(RTZR ~₩1,000/시간)까지 월 수천 원 수준.
3. **한국어 정확도는 영어 리더보드와 다르다.** 공개 한국어 벤치마크(CER) 기준: ReturnZero(RTZR) 5.9% ≥ Clova 7.5% > Whisper large-v3 11.4% > 글로벌 API들. 한국어 특화 엔진은 **키워드 부스팅**(프로젝트 용어집 직결)과 화자분리를 내장.
4. **CPU 노트북에서 완전 로컬은 오디오 1시간당 1~3시간.** 가능은 하지만(aTrain이 증명) 야간 배치용. 로컬 화자분리는 sherpa-onnx(pyannote segmentation-3.0 ONNX 6.6MB + CAM++ 임베딩)로 하면 PyTorch 없이 수 분 내 처리 — Vibe가 실전 증명.
5. **저사양 Windows 노트북에서 Docker는 손해다.** WSL2 vmmem이 RAM의 최대 50%를 점유하고 잘 반환하지 않음. MCP 서버는 그냥 stdio 프로세스이므로 **Python venv(uv)** 가 맞는 체급. Docker는 나중에 상시 서버/NAS로 옮길 때 선택지.
6. **MCP에서 1시간 전사를 하나의 툴 콜로 블로킹하면 안 된다.** 표준 async(MCP Tasks, SEP-1686)는 2026년 중반 현재 Claude 클라이언트 미지원 → `submit_job → job_id / get_status / get_result` 3종 툴 패턴 + SQLite 작업 큐로 구현.
7. **회의록 생성은 서버가 아니라 Claude(클라이언트)가 하는 게 맞다.** MCP 서버는 화자명이 붙은 전사본과 프로젝트 컨텍스트를 돌려주고, 회의록(요약/결정사항/액션아이템)은 Claude Code가 작성 → 별도 LLM API 키·비용 불필요, 프로젝트별로 회의록 스타일 조정 가능. **지금 수동으로 하는 Gemini 업로드→요약 단계가 통째로 사라진다.**
8. **컨텍스트 주입은 3층으로 나눠야 한다** (모든 상용 도구·논문이 수렴하는 패턴):
   - L1 ASR 부스팅: 상위 50~100개 핵심 용어만 (Whisper hotwords 223토큰, Clova ~500개, RTZR 100개 등 하드캡 존재. 과다 부스팅은 오히려 정확도 저하 — 벤더 공식 경고)
   - L2 LLM 후보정: 전체 용어집 + 원문 전사 → "용어집과 발음이 유사한 오인식만 교정, 절대 의역 금지" 프롬프트. 엔진 무관, 시간당 수십 원, **한국어 전문용어 정확도의 실질 주력**
   - L3 회의록 생성 프롬프트: 같은 컨텍스트를 요약 단계에 재사용
9. **법적 제약(중요): 화자 음성 임베딩은 개인정보보호법상 민감정보(생체인식정보)다.** 동료의 목소리로 이름을 식별하는 보이스프린트는 로컬 저장이라도 **당사자 명시적 동의가 필요**. → 기본은 익명 Speaker N(합법), 이름 붙은 프로필은 동의 후 등록하는 UX로 설계하면 출시 가능. 부가로: 무료(소비자용) Gemini에 회의 녹음을 올리는 현재 워크플로우는 구글이 학습·인적 검토에 쓸 수 있는 조건이라 **전체 아키텍처에서 가장 위험한 부분** — 어떤 대안(유료 API, 한국어 엔진, 로컬)으로 가든 개선이다.
10. **화자 학습의 현실적 정확도:** 원거리 마이크·기기 변경 시 임베딩 매칭 오류율이 2배로 뛴다(FFSVC 벤치마크). 고정 임계값 자동 라벨링은 위험 → **"제안 후 확인(suggest-and-confirm)"** 으로 시작, 화자별 다중 임베딩 뱅크 축적, 연속 N회 확인 후에만 자동 라벨로 승격.

---

## 2. 추천 아키텍처

**하이브리드: 화자 지능은 로컬(프라이버시+학습), ASR·요약은 교체 가능한 어댑터(정확도+속도).**

```
녹음 파일 (m4a/mp3/wav…)
      │
      ▼
[1] ffmpeg 정규화 (16kHz mono) + 품질 측정 (채널/SNR/길이)
      │
      ▼
[2] 화자분리 — 로컬 CPU, sherpa-onnx
    (pyannote segmentation-3.0 ONNX + CAM++ 임베딩, 수 분/시간)
      │
      ├──▶ [3] 화자 식별 — 로컬
      │        구간별 임베딩 → SQLite 보이스프린트와 코사인 매칭
      │        미확인 화자는 "이 사람이 ○○○인가요?" 제안 → 확인 시 학습
      │
      ▼
[4] ASR — 어댑터 (설정으로 교체)
    기본:   한국어 특화 클라우드 (RTZR 또는 Clova Speech, 키워드 부스팅 L1)
    무료:   Gemini Flash 무료티어 / Groq Whisper (부스팅은 프롬프트로)
    민감:   faster-whisper large-v3-turbo int8 로컬 (야간 배치 큐)
      │
      ▼
[5] 병합: 단어 타임스탬프 × 화자 구간 → 화자명 붙은 전사본
      │
      ▼
[6] LLM 후보정 (L2): 전체 용어집으로 전문용어·이름 교정
      │
      ▼
[7] 회의록 생성 — MCP 클라이언트(Claude Code)가 수행 (L3)
    요약 · 결정사항 · 액션아이템(담당자) · 필요 시 Notion 발행
```

### 2.1 패키징

- **Python venv (uv) + FastMCP, stdio MCP 서버.** Docker 아님(§1.2-5).
- 전사 작업은 서버 내 백그라운드 워커 + **SQLite 작업 큐** — Claude 세션이 끝나도 작업은 계속.
- 스토리지: SQLite 파일 3개 영역 — `speakers`(이름+임베딩 뱅크+동의 기록), `projects`(용어집·약어·참석자 명단·회의록 템플릿), `jobs`(상태·결과 경로).
- 파일 입력: 절대 경로 인자 (Claude Code에서 자연스러움) + 감시 폴더(inbox) 옵션. 결과는 파일 경로 + 미리보기로 반환 (1시간 전사 전문을 컨텍스트에 넣지 않음).

### 2.2 MCP 툴 설계 (초안)

| 툴 | 역할 |
|---|---|
| `submit_transcription(file_path, project_id, options)` | 작업 등록, 즉시 job_id 반환 |
| `get_job_status(job_id)` | 진행 단계/ETA |
| `get_transcript(job_id)` | 화자명 붙은 전사본 (파일 경로 + 미리보기) |
| `create_project / update_glossary(project_id, terms, participants)` | 프로젝트 컨텍스트 등록 |
| `list_speakers / confirm_speaker(cluster_id, name) / purge_speaker(name)` | 화자 확인·학습·삭제(동의 철회) |

### 2.3 단계별 로드맵

1. **MVP (1~2일):** ffmpeg → sherpa-onnx 화자분리 → ASR 어댑터 1개(무료 클라우드) → 병합 → MCP 3종 툴. 익명 Speaker N.
2. **컨텍스트 층:** 프로젝트 용어집 CRUD + L1 부스팅 + L2 후보정.
3. **화자 학습:** 임베딩 저장소 + suggest-and-confirm + 동의 기록.
4. **품질 루프:** 실제 회의 2~3건으로 개인 평가셋 구축 → 엔진 재선정(한국어+원거리 마이크+한영 혼용은 공개 벤치마크가 전무하므로 자기 데이터로만 판가름 가능).
5. **선택:** Notion 자동 발행, 민감 회의용 완전 로컬 모드, Windows 트레이 앱 래퍼.

### 2.4 예상 성능·비용 (8~16GB RAM, GPU 없음 기준)

| 모드 | 1시간 회의 처리 시간 | 비용 |
|---|---|---|
| 하이브리드(기본) | ~5–15분 (로컬 화자분리가 지배) | 무료~수백 원 |
| 한국어 특화 엔진 | ~5–15분 | ~₩1,000/시간 (RTZR 기준) |
| 완전 로컬(민감) | 1–3시간 (야간 큐) | 0원 |

---

## 3. 구현 전 확정할 것 (→ 확정 내역)

1. **노트북 실제 사양** — ✅ 확정: 기기가 바뀌지만 GPU는 GTX 1050 ~ RTX 3050 범위.
   → 로컬 whisper는 CUDA 자동 감지 + VRAM별 모델 자동 선택(4GB+ → large-v3-turbo,
   3GB → medium, 2GB → small)으로 구현. CUDA 런타임 없으면 CPU int8 폴백.
   RTX 3050에서는 완전 로컬 전사도 실시간보다 빠름 → 로컬 모드의 실용성이 크게 상승.
2. **녹음 환경** — ✅ 확정: 대면은 폰 녹음(원거리 마이크), 온라인은 Zoom 다수.
   → Zoom은 '참가자별 개별 오디오 파일' 로컬 녹음을 켜면 화자 = 파일명이라 화자분리가
   필요 없고, 이때 얻는 깨끗한 트랙으로 보이스프린트를 등록하면 대면 폰 녹음
   인식의 부트스트랩이 된다 (`submit_zoom_tracks` 파이프라인).
3. **클라우드 업로드 허용 범위** — 미확정. 어댑터 구조로 양쪽 다 지원 (기본 클라우드,
   `asr="local"` 플래그로 회의 단위 완전 로컬 전환).
4. **화자 등록 동의 플로우** — `confirm_speaker(consent_confirmed=True)` 게이트로 구현.
   동의 전까지 익명 라벨 유지, `purge_speaker`로 완전 삭제 지원.

## 4. 주의사항 (하드 룰)

- 이 리포는 **공개** 리포다. 임베딩 DB, 전사본, 실명이 든 용어집·참석자 명단은 절대 커밋하지 않는다 (`.gitignore` 필수).
- 원본 오디오는 처리 후 삭제(또는 분리 보관), 화자별 삭제(purge) 기능 내장.
- 무료 소비자용 Gemini에 회의 오디오 업로드 중단 — 유료 API·한국어 엔진·로컬 중 어느 것으로든 대체.
